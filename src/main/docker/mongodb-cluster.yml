version: '2'
services:
    vlcc-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb vlcc-mongodb-config
    vlcc-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    vlcc-mongodb-config:
        image: mongo:3.4.10
        container_name: vlcc-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
